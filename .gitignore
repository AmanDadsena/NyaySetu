# ── Dependencies ──────────────────────────────────────────────
node_modules/
__pycache__/
*.pyc

# ── Environment ──────────────────────────────────────────────
.env
.env.local
.env.*.local

# ── Build output ─────────────────────────────────────────────
.next/
out/
dist/
build/

# ── Python virtualenv ────────────────────────────────────────
backend/venv/

# ── IDE / OS ─────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ── Misc ─────────────────────────────────────────────────────
*.log
npm-debug.log*

# ── Local database (never commit — contains user records) ────
*.db
*.sqlite
*.sqlite3

# ── Retrieval feedback log (never commit — contains users' legal questions) ────
# Written only when NYAYSETU_FEEDBACK_LOG is set. See app/rag/feedback.py.
feedback.jsonl
backend/data/feedback.jsonl

# ── Local debugging scratch (not part of the project) ─────────
/analyze.html
/check_url*.py

# ── Marketing and promotional assets ─────────────────────────
# Kept locally, never pushed. This repository is the product, not the
# material used to talk about it, and a regenerated deck would otherwise
# land in history as a fresh binary on every rebuild.
Nyaysetu-LinkedIn-*
Nyaysetu-Carousel-*
*-carousel.pdf
shots/
