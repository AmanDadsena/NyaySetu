# ── Dependencies ──────────────────────────────────────────────
node_modules/
__pycache__/
*.pyc

# ── Environment ──────────────────────────────────────────────
.env
.env.local
.env.*.local

# ── Build output ─────────────────────────────────────────────
.next/
out/
dist/
build/

# ── Python virtualenv ────────────────────────────────────────
backend/venv/

# ── IDE / OS ─────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ── Misc ─────────────────────────────────────────────────────
*.log
npm-debug.log*

# ── Local database (never commit — contains user records) ────
*.db
*.sqlite
*.sqlite3

# ── Retrieval feedback log (never commit — contains users' legal questions) ────
# Written only when NYAYSETU_FEEDBACK_LOG is set. See app/rag/feedback.py.
feedback.jsonl
backend/data/feedback.jsonl

# ── Local debugging scratch (not part of the project) ─────────
/analyze.html
/check_url*.py

# Marketing copy and its output — kept locally, not part of the source tree
Nyaysetu-LinkedIn-Post.md
Nyaysetu-LinkedIn-Carousel.pdf
