fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic[email]>=2.10.0
python-dotenv>=1.1.0
python-multipart>=0.0.20
PyPDF2>=3.0.0
python-docx>=1.1.0
google-genai>=0.2.0
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
# Postgres driver. Set DATABASE_URL to a Neon/Supabase connection string and
# the app uses it; leave it unset and it falls back to local SQLite.
asyncpg>=0.30.0
passlib[bcrypt]>=1.7.4
bcrypt==3.2.2
python-jose[cryptography]>=3.3.0
httpx>=0.27.0

# Optional — enables cross-lingual semantic retrieval. Everything works without
# it (BM25 plus the lexicon in app/rag/lexicon.py); installing it lets a Tamil
# or Bengali question match the English corpus semantically.
#   pip install sentence-transformers

